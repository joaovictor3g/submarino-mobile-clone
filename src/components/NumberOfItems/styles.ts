import styled from 'styled-components/native';

export const CountItems = styled.View`
    border-radius: 100px;
    width: 18px;
    height: 18px;
    background: red;
    align-items: flex-end;
    position: absolute;
    top: 0;
    right: 0;
    align-items: center;
    z-index: 2;

`;

export const NumberItem = styled.Text`
    font-size: 15px;
    color: #FFF;
`;